"""Bundled SignalCompanion plugins.

Each subpackage exposes a module-level `PLUGIN` instance (or `get_plugin()`)
of a core.plugin.Plugin subclass. Drop a new package here and it is
auto-discovered by core.manager.PluginManager on next start.
"""
